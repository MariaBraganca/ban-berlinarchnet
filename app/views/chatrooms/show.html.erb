<!-- <div class="d-flex"> -->
  <!-- <div class="list-group w-200px bg-white br-light"> -->
    <%# @chatrooms.each do |chatroom| %>
      <%#= link_to "##{chatroom}", chatroom_path(chatroom), class: "list-group-item #{'active' if chatroom == @chatroom}" %>
    <%# end %>
  <!-- </div> -->

<div class="chatroom">
  <h1>Chats</h1>


  <div id="messages" data-chatroom-id="<%= @chatroom.id %>">
    <% @chatroom.messages.each do |message| %>
      <div class="message-container" id="message-<%= message.id %>">
        <i class="author">
          <span><%= message.user.first_name %> <%= message.user.last_name %></span>
          <small><%= message.created_at.strftime("%a %b %e at %l:%M%p") %></small>
        </i>
        <p><%= message.content %></p>
      </div>
    <% end %>
  </div>

    <%= simple_form_for [ @chatroom, @message ], remote: true do |f| %>
    <%= f.input :content, label: false, placeholder: "Message ##{@chatroom}" %>
    <%= f.submit 'Send', :class => 'btn btn-primary' %>
  <% end %>
</div>
